const state = {
  /**
   * The id of the resume to make active.
   *
   * @type {Number}
   */
  active: undefined,

  /**
   * The list of resumes.
   *
   * @type {Array}
   */
  resumes: [],

  /**
   * Determines which template is active for the resume.
   *
   * @type {String}
   */
  template: undefined
};

const mutations = {
  // 
};

const actions = {
  // 
};

const getters = {
  // 
};

export default {
  state,
  mutations,
  actions,
  getters
};
